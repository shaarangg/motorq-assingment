import Layout from "../../components/Layout";
function map() {
	// let courses = classes.filter((cls) => {
	// 	return cls.courseCode;
	// });
	// console.log(courses);
	return <Layout>Map</Layout>;
}

export default map;
